tentacle_corrupt_start_scheme_interaction = {
	icon = icon_hostile
	interface_priority = 70
	category = interaction_category_hostile
	desc = tentacle_corrupt_start_scheme_interaction_desc

	send_name = START_SCHEME

	scheme = tentacle_corrupt
	ignores_pending_interaction_block = yes
	
	is_shown = {
		OR = {
			debug_only = yes #debug模式无所畏惧
			AND = {
				scope:actor = { is_tentacle_slave = yes }
				NOR = {
					scope:recipient = scope:actor
					scope:recipient = { is_imprisoned = yes }
				}
			}
		}
	}

	is_valid_showing_failures_only = {
		scope:actor = {
			can_start_scheme = {
				type = tentacle_corrupt
				target = scope:recipient
			}
		}
		scope:recipient = { NOT = { has_strong_hook = scope:actor } }
	}

	on_accept = {
		scope:actor = {
			stress_impact = {
				compassionate = medium_stress_impact_gain
				honest = minor_stress_impact_gain
				just = minor_stress_impact_gain
			}
		}
		hidden_effect = {
			scope:actor = {
				send_interface_toast = {
					title = start_tentacle_corrupt_notification

					left_icon = scope:actor					
					right_icon = scope:recipient

					start_scheme = {
						type = tentacle_corrupt
						target = scope:recipient
					}

					show_as_tooltip = {
						stress_impact = {
							compassionate = medium_stress_impact_gain
							honest = minor_stress_impact_gain
							just = minor_stress_impact_gain
						}
					}
				}
			}
		}
	}
	
	auto_accept = yes
	
	ai_potential = {
		always = no
	}
	ai_frequency = 12
	
	ai_targets = {
		ai_recipients = family
		max = 2
	}
	ai_targets = {
		ai_recipients = guests
		ai_recipients = courtiers
	}
	
	ai_targets = {
		ai_recipients = realm_characters
		max = 10
	}
	
	ai_target_quick_trigger = {							# Quick triggers for the ai_targets
		adult = yes
	}
	ai_will_do = {
		base = 0
	}
}
tentacleswarm_imprisoner_interaction = {
	category = interaction_category_prison
	common_interaction = yes
	interface_priority = 100
	desc = tentacleswarm_imprisoner_interaction_desc
	
	ai_frequency = 3 #AI多久考虑一次
	
	ai_targets = {
		ai_recipients = prisoners
	}

	is_shown = {
		NOT = { scope:actor = scope:recipient }
		scope:actor = { 
			OR = {
				is_tentacle_slave = yes
				has_religion = religion:tentacleswarm_religion
			}
		}
		scope:recipient = { is_imprisoned_by = scope:actor }
	}

	is_valid = {
		scope:recipient = { 
			is_tentaclable_female = yes
			is_tentacle_slave = no 
		}
	}
	
	is_highlighted = {
		scope:recipient = { 
			is_tentaclable_female = yes
			is_tentacle_slave = no 
		}
	}
	on_send = {
	}
	on_accept = {
		scope:actor = { save_scope_as = imprisoner }
		scope:recipient = {
			trigger_event = {
				id = tentacleswarm_slave.0041 #被囚禁者腐化
			}
			show_as_tooltip = {
				become_tentacle_slave = yes
			}
		}
	}
	
	ai_potential = {
		is_tentacle_slave = yes
	}
	
	auto_accept = yes
	
	ai_will_do = {
		base = 100
	}
}